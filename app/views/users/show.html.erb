<main>
    <%= render partial: 'shared/user_card', locals: {user: @user}%>
    <article>
        <p>Bio: <%= @user.bio %></p>
    </article>
    <%= render partial: 'shared/post_card', collection: @user.recent_post, as: :post%>
</main>